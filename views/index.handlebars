{{#each allBurgers}}
    <div class="row">
        <div class="col">
            <p class="lead">{{burger_name}}</p>
        </div>
        <div class="col">
            <button type="button" class="btn btn-secondary">Devour it!</button>
        </div>
    </div>
{{/each}}